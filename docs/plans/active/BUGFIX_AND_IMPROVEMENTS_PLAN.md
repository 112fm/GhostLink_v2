# BUGFIX_AND_IMPROVEMENTS_PLAN

## Статус

Активный рабочий план стабилизации прода и egress-маршрутизации.
Обновлено: 16.06.2026.

## P0 (критично сейчас)

1. [x] Стабилизированы оплаты:
- `payment report` уходит в `pending_verification`;
- подтверждение админом продлевает подписку на `+30 дней` от текущего срока;
- API и бот работают с одной БД.

2. [x] Стабилизирована базовая логика ключей:
- `device/add`, `device/remove`, `device/rotate` рабочие;
- подписочные ссылки (`/s/<token>`) валидны для активных устройств.

3. [ ] Закрыть кейс "оплатил апгрейд устройств, лимит не вырос":
- хранить `target_device_limit` в момент платежа;
- при `approve payment` применять целевой лимит в БД и синхронно в панели;
- исключить зависимость лимита от повторного `device/add`.

4. [ ] Устранить плавающие `502` при `POST /api/device/add`:
- расширить серверные причины ошибок в логах (`panel_add_failed` код/текст);
- вернуть пользователю понятный текст ошибки вместо `Failed to fetch`.

5. [ ] Финальный живой smoke-тест (2-3 реальных клиента):
- сценарии: добавление устройства, оплата, подтверждение, импорт ключа.

6. [ ] Срочно восстановить вход в оригинальную X-UI панель через временный proxy-доступ:
- вход только через кнопку администратора и подписанный токен с ограниченным TTL;
- проверить загрузку HTML, CSS, JS, API-запросов и WebSocket через `panel.112prd.ru`;
- исключить зависимость ассетов панели только от `Referer` или cookie браузера;
- после исправления проверить открытие, продление и принудительное закрытие сессии;
- прямой неавторизованный запрос к `/panel/` и `/panel/assets/*` должен возвращать `403`.

## P0.1 (сетевой контур: анти-капча/анти-регион)

Цель: развести конфликтующие требования RU-сервисов и AI-сервисов.

1. [ ] Принять целевую схему маршрутизации:
- RU-сервисы (`ozon`, `yandex`, `vk`) -> `direct/bypass` (домашний IP клиента).
- AI-сервисы (`openai`, `chat.openai`, `gemini`, `googleapis`, `gstatic`, `accounts`) -> `warp`.
- Остальное -> текущий `direct`.

2. [ ] Применить в x-ui:
- outbound `warp` (`socks 127.0.0.1:40000`);
- routing-rule только для целевых AI-доменов (без широких масок типа `geosite:ru`).

3. [ ] Проверить фактический трафик:
- runtime-проверка соединений к `127.0.0.1:40000`;
- 1 тест на Gemini/OpenAI + 1 тест на RU-приложениях.

## P1 (важно)

1. [ ] Улучшить админ-навигацию по пользователям:
- быстрый поиск по `tg_id`, `username`, имени;
- связка "пользователь -> его устройства -> что онлайн".

2. [ ] Дошлифовать UX админки по проблемным кейсам:
- быстрые фильтры по платежам/статусам;
- очевидные действия для ручной поддержки.

2.1. [ ] Разработать матрицу прав администраторов:
- определить роли и список разрешенных разделов Mini App для каждой роли;
- разделить просмотр пользователей, платежей, ключей, панели, статистики и системных настроек;
- определить, какие уведомления бот отправляет владельцу, платежному администратору и техническому администратору;
- исключить попадание чувствительных данных, токенов, ключей и лишней персональной информации в уведомления;
- добавить серверную проверку прав для каждого admin API, а не только скрытие кнопок во frontend;
- подготовить сценарии проверки доступа для каждой роли.

3. [ ] Дошлифовать UI блока "Мои ключи" (mini в приоритете):
- выравнивание и читаемость на узких экранах;
- без изменения текущей бизнес-логики.

4. [x] Пересмотреть цены Solo Ghost на 1/2/3 месяца:
- поднять цены для Solo-периодов без изменения Flex-логики;
- синхронизировать backend price matrix, mini-app fallback prices и тексты оплаты;
- проверить quote/payment/report/approve сценарии после изменения.

Результат 16.06.2026:
- Solo Ghost regular: `1 мес = 150 ₽`, `2 мес = 290 ₽`, `3 мес = 430 ₽`;
- изменены `backend/domain/billing.py` и `webapp-mini-v2/src/modules/payments.js`;
- backend-файл залит на сервер вручную, `deploy_safe.sh` завершился с `exit_code=0`;
- Mini App fallback-цены запушены в GitHub: `f0f179d Update Solo Ghost period fallback prices`;
- Flex и бонусные группы (`referral`, `own/vip`) не менялись.

5. [ ] Обновить рекомендации по VPN-приложениям:
- протестировать INCY на iOS/Android как рекомендуемый основной клиент;
- проверить импорт обычного VLESS и subscription URL;
- проверить работу на мобильной сети и Wi-Fi;
- после теста обновить тексты бота, Mini App и инструкции по приложениям;
- Karing не удалять: оставить доступным как дополнительный клиент с отдельной инструкцией и файлами настройки.

6. [ ] Подготовить миграцию с Aeza на Hetzer:
- зафиксировать дату дедлайна Aeza: до 15 числа;
- поднять и проверить новый контур на Hetzer;
- перенести API/бот/x-ui/БД/сертификаты/Cloudflare DNS по runbook;
- сделать smoke-тест ключей, оплат, bridge и админки до переключения пользователей.
- целевая схема: `Hetzner = основной прод`, `Aeza = теплый ручной резерв`;
- не делать автоматический failover на первом этапе;
- подготовить простой ручной аварийный сценарий `одна команда на Aeza -> вернуть DNS и сервисы на резерв`;
- вынести этот сценарий в отдельный `failover_manual.sh` с Telegram-уведомлением админу;
- настроить мониторинг доступности Hetzner, который шлет alert, но не переключает DNS автоматически;
- после стабилизации Hetzner отдельно решить, нужна ли частичная автоматизация failover.

7. [ ] P0 по Hetzner перед миграцией:
- создать отдельного sudo-пользователя для администрирования;
- оставить вход по SSH-ключу, отключить парольный вход;
- не завязывать SSH-защиту на allowlist IP, так как IP администратора плавающий;
- включить базовый firewall (`22`, затем `80/443` по мере запуска сервисов);
- установить `fail2ban`;
- добавить swap;
- только после этого переносить приложение и данные.

8. [ ] Протестировать Hysteria2 как дополнительный протокол:
- поднять тестовый Hysteria2 без вмешательства в рабочий VLESS;
- проверить обнаруживаемость/устойчивость на мобильных сетях РФ;
- проверить поддержку в целевых клиентах (INCY/Karing/Happ/V2);
- принять решение, добавлять ли `hysteria2://` строки в существующую `/s/<sub_token>` подписку.

8.1. [ ] Разобрать устойчивость VPN на проблемном домашнем LTE/Wi-Fi:
- текущий симптом: GhostLink не проходит или сильно тупит на домашнем Wi-Fi через LTE-роутер/MIMO/SIM, при этом на телефоне через мобильный интернет работает нормально;
- сравнить с VPSUS/VPN Giant, у которого VLESS/TCP-подписка работает в тех же условиях;
- задачу вести по отдельному backlog-плану: `docs/plans/backlog/NETWORK_RESILIENCE_AND_VPSUS_BENCHMARK_PLAN.md`;
- подписочные URL/ключи не коммитить и не хранить в docs в открытом виде.

9. [x] PWA-контур выведен из работы:
- `webapp-pwa/` удален из репозитория;
- PWA как устанавливаемое приложение отменен, целевая модель: обычный Bridge-сайт;
- базовый bridge-сценарий вынесен в отдельный модуль invites/bridge;
- дальнейшее развитие Bridge ведется отдельно от P0 стабилизации прода.

10. [x] legacy mini-v1 удален:
- `webapp-mini/` удален из репозитория;
- единый продовый фронт: `webapp-mini-v2/`.

11. [x] Bridge-проект вынесен в backlog-док:
- `docs/plans/backlog/BRIDGE_SHADOW_PLAN.md`;
- в активном плане Bridge не реализуется, только фиксируются зависимости и условия старта.

## P2 (техдолг и структура)

1. [x] Не развивать `mini/pwa`: контур закрыт и удален из репозитория.

2. [ ] Рефакторинг `api_server.py` и `ghost_final.py` этапами:
- этап 1: выделить слои `payments`, `devices`, `auth`, `admin`;
- этап 2: убрать дубли и мертвые ветки, стандартизовать ошибки;
- этап 3: покрыть критичные сценарии smoke/integration тестами.

3. [ ] Подготовка резервного контура (DR-режим) и документирование.
