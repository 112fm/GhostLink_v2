# Allow-list mode: ignore everything by default.
*

# Keep git metadata file itself tracked.
!.gitignore

# Allow only frontend folders that must be in GitHub.
!webapp-mini/
!webapp-mini/**
!webapp-pwa/
!webapp-pwa/**
!webapp-shared/
!webapp-shared/**
